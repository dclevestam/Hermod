# Hermod Roadmap

Phased plan for the rest of the build. Each phase has a concrete **goal**,
a **scope** list (what is in / out), **acceptance criteria**, and the
**files** most likely to change. Order is deliberate: finish the phase in
front before starting the next.

The roadmap is the working plan. When a phase lands, promote its
decisions into `CHANGES.md` and retire the phase from this file.

---

## Legend

- Status tags: `NEXT` (work starts here), `QUEUED`, `LATER`, `PARKED`.
- "Ship gate" means the phase is done for external eyes; internal
  polish can still land under a later phase.

---

## Phase 0 — Design pass (IN FLIGHT)

**Status:** mostly done, a handful of polish items open.
**Goal:** make the live GTK app match the design prototype in
`/home/david/Downloads/Hermod Images/Hermod Design Claude.zip`.

Done in this pass:

- Design tokens in `styles.py` (`ACCENT_PALETTE`, `DAY_PALETTES`,
  `DENSITY_ROW_HEIGHT`, `build_theme_override_css`)
- Settings Appearance section (theme / day variant / accent / density /
  AI toggle) with live apply via `window.apply_theme()`
- Welcome screen rewritten to the two-column layout with its own
  Adw.HeaderBar ("H HERMOD" brand)
- H mark sized correctly via `Gtk.Image.new_from_file` + `pixel_size`
- More Providers dialog rewritten as a `Gtk.Window` with stripped OS
  chrome and the shared `_build_modal_shell` head
- Connect Gmail / Connect <provider> setup dialog now uses eyebrow +
  title + subtitle + close pattern
- Banding-prone low-alpha gradients flattened on the photo panel,
  search shell, startup panels, reading pane, sidebar, message column,
  attachment and reply bars

Still open under Phase 0:

1. **Welcome photo asset** — *done.* `assets/welcome-photo.png` is
   generated by `tools/generate_welcome_photo.py` (pre-dithered forest
   gradient + teal/forest aurora radials) and wired into the welcome
   photo column as a `Gtk.Picture` inside a `Gtk.Overlay`. The caption
   rides on top as a `Gtk.Overlay` child so the move controller still
   gets pointer events for the whole panel. Real photography can
   replace the PNG later without touching the panel wiring.
2. **Window move on welcome** — *verified 2026-04-19.* The welcome
   uses `Adw.HeaderBar` on a plain `Gtk.Box`; drag-to-move is
   delivered by `_attach_window_move_controller` at
   `window_welcome.py:527`, which wires a `Gtk.GestureClick` on the
   photo overlay into `surface.begin_move()`. Works regardless of
   parent (no ToolbarView wrapping needed).
3. **Accounts list polish** — the in-sidebar ACCOUNTS row is done
   (status dot + chevron + sans-serif label, see `CHANGES.md`); the
   Settings "Accounts added" section still needs a row-tokens /
   spacing pass against the prototype.
4. **Compose surface** — *verified 2026-04-19.* `COMPOSE_CSS` uses
   `@hermod_accent` / `@hermod_bg_elevated` / etc via
   `@define-color`; `window._apply_css()` rebuilds the theme
   override provider (via `build_theme_override_css`) and reloads
   it, which cascades the new tokens to all providers — including
   the compose provider — so compose retints live on theme change.
5. **Reader header** — covered under sub-phase D below (`window.py`
   `_message_info_bar` redesigned to consume
   `build_theme_override_css` tokens).

### Main-window design alignment (sub-phase)

The inbox shell still carries pre-design chrome (centered header
title, big `ONLINE` pill beside Compose, flat folder list, no day
grouping). Ordered smallest → largest; each ships independently:

6. **Header bar (A)** — *done.* `window.py` has a new
   `_HeaderTitleStrip` widget (`H` mark + `HERMOD` wordmark + vertical
   separator + folder crumb) left-packed via `Adw.HeaderBar.pack_start`;
   the settings gear (`emblem-system-symbolic`) and a small refresh
   icon are right-packed via `pack_end`; the centered Adw.WindowTitle
   is replaced with an empty `Gtk.Box`. Existing `set_title` /
   `set_subtitle` call sites keep working because the strip exposes
   the same two setters. Styled in `styles.py` under `.hermod-header*`
   class tokens.
7. **Sidebar (B)** — *done.* `window.py` now ships a full-width teal
   Compose with a `Ctrl N` kbd chip at the end and no standalone
   `ONLINE` button beside it (the sync pill moved to the header under
   sub-phase A). `window_message_list._populate_sidebar` groups rows
   under a new `SidebarSectionRow` eyebrow widget (`widgets.py`): the
   unified `All Inboxes` / `All Trash` / `All Spam` rows sit under
   `MAILBOXES` and the per-account header + folders + more-row sit
   under `ACCOUNTS`. Styled in `styles.py` under `.sidebar-compose-chip`
   and `.sidebar-section*` tokens. The flat row set landed
   2026-04-19 (`All Inboxes / Flagged / Drafts / Sent / Archive /
   Trash`, with Flagged routing through the segmented filter and
   the others through `_load_unified_folder`); count badges for the
   new rows still pending (Drafts/Sent/Archive unread buckets need
   provider-side counting).
8. **Message list (C)** — *done.* The message column now carries a
   `_message_column_header` Box at the top with an eyebrow label
   (folder crumb uppercased, defaults to `ALL INBOXES`), a meta line
   (`N messages · N unread`), and a `Unified / Unread / Flagged`
   segmented chip group (`Gtk.ToggleButton` in a radio group with
   `.message-filter-chip.selected` CSS state); selecting a chip calls
   `MessageListMixin.set_filter_mode()` which updates
   `_filter_mode`, keeps the legacy `_show_unread_only` toggle in
   sync, and fires `_message_filter.changed(DIFFERENT)`. Day grouping
   is handled via a new `DayGroupListItem` / `DayGroupRow`
   (`widgets.py`) — `_build_message_items` injects headers between
   messages when the local date changes and back-references the
   following `MessageListItem`s via `followers` so `_email_filter`
   hides orphan group headers when no follower passes the active
   filter. Keyboard navigation (`_move_selection`) now skips day-group
   items. Unread dots + thread-count pills already existed from the
   Phase 0 baseline; styling was left untouched. Styles: new tokens
   `.message-column-header`, `.message-column-eyebrow`,
   `.message-column-meta`, `.message-filter-segmented`,
   `.message-filter-chip`, `.day-group-row`, `.day-group-label`. The
   old sort/unread toolbar was retired from the UI; the
   `_sort_toggle_btn` / `_unread_toggle_btn` stubs remain invisible so
   existing tests and key-bindings keep working.
9. **Reader (D)** — *code-complete.* The reader header (`window.py`
   `_message_info_bar`) now stacks a large `.reader-subject`
   headline on the left with a top-right `.reader-actions` cluster
   (reply / reply-all / forward / delete) and a `.reader-meta` line
   below it. `_update_message_info_bar` (`window_reader.py`)
   populates the meta as `sender · received-date` for single
   messages; `_render_thread_view` populates it as `N messages ·
   participants`. The old `Received: …` and `Size · N attachments`
   lines are retired (labels remain in the tree as hidden fallbacks
   so tests / external callers do not break). Forward is wired:
   `compose.ComposeView(forward_from=msg)` pre-fills `Fwd: <subj>`
   + `---------- Forwarded message ----------` attribution block +
   `>`-quoted body; reader button + keyboard `f` both open it.
   Attachments auto-include remains a follow-up. Styles:
   new tokens `.reader-header`, `.reader-subject`, `.reader-meta`,
   `.reader-actions`, `.reader-action-btn` in `styles.py`, plus a
   bumped `.message-info-subject` size to match. Subsumes the old
   item 5 (reader header tokens). Smart-reply row defers to Phase 3.
   **Visual verification:** confirmed via dump-ui 2026-04-19 after
   the `_set_error` stub in `window_mailbox_controller.py` was added
   (pre-existing bug that masked reader rendering). Subject headline,
   action cluster, and `sender · date` meta render as designed.
   Files touched: `window.py`, `window_reader.py`, `styles.py`,
   `window_mailbox_controller.py`.

**Acceptance:** every screenshot in
`/home/david/Downloads/Hermod Images/` has a matching live dump-ui
artifact.

### Design resync (sub-phase, 2026-04-20)

The design folder gained higher-fidelity screenshots
(`design_handoff_hermod/screenshots/Inbox Night Corrected.png`,
`Setting apperance.png`, `Settings Accounts.png`,
`Settings Intelligence.png`, `Setting Notifications.png`) that
expose deltas the lower-res originals hid. Ordered smallest →
largest; done in this order:

10. **Reader body blank render — root cause found and patched
    (2026-04-20 evening).** The reader body went blank whenever
    `_startup_status_active` remained `True` (e.g. because one
    backend had a sync issue, keeping the startup panel in
    "blocking attention" state at `window_message_list.py:1107-1122`).
    In that mode `_show_mail_view` routes the viewer stack back to
    `"startup-status"` on every selection — so
    `_render_body` / `_render_thread_view` load their HTML into a
    hidden stack child while the "Starting mail" panel covers the
    reader. Fix: `_commit_email_selection`
    (`window_message_list.py:513-521`) now calls
    `_clear_startup_status_view()` first when a user explicitly
    clicks a row, forcibly flipping the viewer stack to `"viewer"`
    before the body render happens. User action = implicit
    dismissal of startup overlay. Needs live verification.
11. **Orphan inline reply tray — done.** `window_reader.py:616`
    now sets `_thread_reply_bar.set_visible(False)` unconditionally;
    the feature plumbing is preserved but the tray is hidden so
    the reader matches the design (reply uses the top-right action
    cluster and the full compose surface via `_on_current_reply`).
12. **Settings restructure — done + promoted to modal window
    (2026-04-20 afternoon).** `settings.py` now builds a paginated
    Preferences dialog (eyebrow + title + close X header, left
    ListBox nav, right Stack of six panes: Accounts / Appearance /
    Intelligence / Notifications / Shortcuts / About). **The
    Preferences content is no longer mounted inside
    `_viewer_stack`** — it lives in a modal `Gtk.Window` (decorated
    off, 1020×680, transient_for=main) built lazily by
    `HermodWindow._present_settings_modal()`. The gear button in
    `_on_settings` now toggles that modal; the close X wires into
    `_close_settings_modal`. Day-variant selector restored to the
    Appearance pane (visible when `theme_mode == "day"`, hidden
    otherwise; the Night/Day segmented toggle switches it
    live). Accounts preserves the inner main↔editor Stack wired
    via `AccountSettingsController`. Legacy Reading / Behavior /
    Sidebar / Background Updates / Debug sections live under About
    → "Show advanced preferences" disclosure. New settings keys
    added to `DEFAULTS` (`notifications_enabled`, `quiet_hours_*`).
    `HERMOD_FORCE_SETTINGS=<pane_id>` dump-ui helper is kept; note
    dump-ui snapshots the main window only, so the modal Preferences
    window does not appear in the dump (verify interactively).
    **Original spec preserved below.** Replace the single
    scrollable page in `settings.py` (Appearance / Reading /
    Behavior / Sidebar / Background Updates / Debug) with a
    **Preferences dialog** matching the prototype:
    - Chrome: `PREFERENCES` eyebrow + large `Settings` title +
      close X
    - Left nav: `Accounts · Appearance · Intelligence ·
      Notifications · Shortcuts · About`
    - **Appearance** pane slimmed to: Theme (Night/Day pill) +
      Highlight colour (Deep Teal / Forest / Woodland Gold /
      Stone) + Density (Comfortable / Balanced / Compact). The
      current Reading / Behavior / Sidebar sections fold into an
      "Advanced" disclosure under About, or relocate behind the
      relevant pane.
    - **Accounts** pane: provider-dot + name + `Provider · synced
      X min ago` + green health dot + Edit; `+ Add account` chip
      at bottom. Close to `settings_accounts.py` today; needs
      card-row re-skin.
    - **Intelligence** pane (new): Assist features toggle +
      subtitle `Summaries, smart replies, and extraction. Never
      leaves your machine.` + Model row (`LM Studio ·
      mistral-7b-instruct · 4.2 GB · running`) + `• Ready` pill.
      Infrastructure lives in Phase 3 but the settings surface
      ships here so the toggle exists when AI lands.
    - **Notifications** pane: Native desktop notifications
      toggle + subtitle `Uses org.freedesktop.Notifications.` +
      Quiet hours row with `22:00 – 07:00 · weekdays` + Edit.
    - **Shortcuts** pane and **About** pane: thin stubs for now
      (Shortcuts references Phase 5's `Ctrl+?` dialog; About
      carries version + license).
13. **Unread accent bar — done (code; visual pending reauth).**
    `EmailRow.__init__` (`widgets.py`) now adds `unread` class to
    the row box instead of showing an 8px dot widget; `mark_read`
    / `mark_unread` toggle the class. `styles.py` replaces
    `.unread-dot` with `.message-list-view .email-row.unread
    { box-shadow: inset 3px 0 0 0 #2e6a70 }`, matching the selected
    state bar. Row `outer` margin_start bumped 10→13 for the
    bar-to-content gap. Visual dump-ui confirmation blocked by the
    same OAuth sync-issue dialog as item 10.
14. **Reader action cluster trim — done.** Dropped
    `_reader_reply_all_btn` from `window.py:1163-1171`; header now
    shows Reply / Forward / Delete (3 icons, down from 4). `a`
    keybind wiring and `_on_current_reply_all` method kept intact
    so the feature still works via keyboard and message-list
    action menu.

Retired / superseded: none. Items 1–9 of the Phase 0 list above
stay as-is.

**Goal:** all eight onboarding tiles lead to a working account, not
just Gmail / IMAP.

Scope (in):

- **Microsoft Graph** — activate `providers/microsoft_graph.py` or
  create it under `providers/`, register through `backends.py`, wire
  up the OAuth flow in `accounts/auth/`, and surface it through
  `settings_accounts.py`. Target feature parity with Gmail API path
  (list, body, flags, unread counts, send).
- **Proton Bridge** — IMAP/SMTP through local bridge; detect bridge
  availability and surface a helpful error when it is off.
- **iCloud Mail** — IMAP + app password flow; capture the
  app-password recipe in the setup dialog subtitle.
- **Fastmail, Yahoo, Zoho** — IMAP + app password recipes.
- **Exchange (on-prem)** — EWS via IMAP-compatible gateway; defer
  native EWS.

Scope (out): Exchange Web Services native, Office 365 enterprise
tenants with conditional-access policies.

**Acceptance:**

- Each provider in `ALL_PROVIDERS_ORDER` (see
  `window_welcome.py:56`) leads to a dialog that can reach "connected"
  on a real account.
- `tests/test_provider_*` covers the listing/body/flag path for the
  new providers using recorded fixtures.
- Provider art under `assets/providers/` is normalised (PNG for the
  few that need real logos, SVG letter glyphs as fallback).

**Files:** `providers/*`, `backends.py`, `accounts/registry.py`,
`accounts/auth/*`, `settings_accounts.py`, `window_welcome.py`,
`tests/test_provider_*.py`.

---

## Phase 2 — Reader / thread polish (QUEUED)

**Goal:** the reading pane is the flagship surface; it should feel
settled before intelligent features layer on top.

Scope:

- Reader pane uses available width without a dead side column
  (carryover from BUGS.md #3).
- Inline image rendering confirmed across multi-image messages
  (BUGS.md #4).
- Thread-member edge cases (BUGS.md low-priority #1) handled —
  absent-members, single-message "threads", self-reply chains.
- Thread drawer open/close animation matches design timings.
- Attachment chip hover/active states match tokens.
- Quoted-text collapse has a visible affordance.

**Acceptance:** manual verification against three canonical real
threads (single message, long thread, multi-image marketing mail)
with dump-ui snapshots saved under `diagnostics/`.

**Files:** `window_reader.py`, `window_reader_controller.py`,
`thread_renderer.py`, `styles.py`.

---

## Phase 3 — Intelligent features, local-only (LATER)

Drives the items in `POTENTIAL_FEATURES.md`. This phase is gated on
Phase 0 and Phase 2 finishing because the reading pane is where
intelligence renders.

Infrastructure first:

- **Local model runtime** — settle on LM Studio over `localhost` via
  an OpenAI-compatible endpoint; add `hermod.ai` module with a thin
  client, config keys in `settings.py` (`ai_endpoint`,
  `ai_model_name`, plus existing `ai_enabled`).
- **Embedding store** — decide between sqlite-vec and a flat FAISS
  cache under XDG cache; wrap both behind a `hermod.ai.embeddings`
  interface.
- **Redaction before model calls** — route every prompt through
  `diagnostics.redaction` to strip obvious secrets.

Feature order:

1. **Thread summarisation** — TL;DR, pending actions, status, stamped
   into the thread drawer. Only runs when `ai_enabled`.
2. **Smart reply drafting** — three suggested replies above the
   compose surface; generated on demand, not streamed into the text
   area until the user accepts.
3. **Intent & action detection** — extract dates and action-ish
   phrases, surface a "looks like a task" row above the reader.
4. **Data extraction chips** — tracking numbers, flight codes, meeting
   links pinned above the message body.
5. **Semantic search** — vector index of subjects + bodies; NL query
   box in the sidebar.
6. **Priority ranking** — local classifier bubbles "deep work" vs
   "newsletter"; affects list sort when opted in.

**Acceptance:**

- All features are opt-in behind `ai_enabled`.
- No network egress beyond the configured local endpoint — verified
  in `tests/test_ai_*` by asserting no outbound requests except to the
  configured host.
- Each feature has a visible kill switch in Settings.

**Files (new):** `hermod/ai/`, `tests/test_ai_*`.
**Files (edit):** `settings.py`, `window_reader.py`, `compose.py`,
`window_message_list.py`, `styles.py`.

---

## Phase 4 — Packaging and distribution (LATER)

**Goal:** someone other than David can install Hermod.

Scope:

- Flatpak manifest (`io.github.hermod.Hermod`) under `packaging/`.
- Appstream metadata + `.desktop` file.
- Release pipeline: tag → Flathub submission checklist.
- Optional: AppImage for people who avoid Flatpak.

**Acceptance:** `flatpak-builder` build passes in a clean container
and the installed app launches and signs a real account in.

**Files (new):** `packaging/*`, `.github/workflows/release.yml`.

---

## Phase 5 — Accessibility and keyboard pass (LATER)

Scope:

- Every interactive element reachable via keyboard.
- Screen reader labels on provider tiles, accent swatches, account
  rows.
- High-contrast mode variant under `day_variant`.
- Font scaling respected.
- Shortcut cheatsheet dialog (`Ctrl+?`).

**Files:** `window*.py`, `styles.py`, new `shortcuts.py`.

---

## Parked / open research

- Calendar and contacts integration — not yet in scope; would need a
  separate provider abstraction.
- Real EWS native provider — wait for real demand.
- Encryption (PGP / S/MIME) — only after Phase 1 is settled.
- Mobile / GNOME Mobile shell — parked until Phase 4 ships.

---

## Per-phase exit ritual

1. Promote the phase's "done" bullets into `CHANGES.md` under a new
   top-level section.
2. Retire the phase from this file (delete or mark `LANDED`).
3. Regenerate architecture artifacts:
   ```bash
   python3 tools/update_architecture.sh
   python3 tools/check_architecture_contracts.py
   ```
4. Run the relevant portion of `VERIFICATION.md` and log the result.
5. Update `/home/david/AI/hub/projects/Hermod/CURRENT.md` so the hub
   matches the repo.

---

## Cross-reboot continuity

After reboot, resume from:

- This file (`ROADMAP.md`) for the plan.
- `CHANGES.md` for fixed behaviours that must not regress.
- `VERIFICATION.md` for the smoke ritual.
- `/home/david/.claude/projects/-home-david/memory/handover.md` for
  the short "what was I doing" note.
- `/home/david/AI/hub/projects/Hermod/CURRENT.md` for live hub state.
